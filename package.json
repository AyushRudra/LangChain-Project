{
  "name": "emoji-explainer-with-openai",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "dependencies": {
    "dotenv": "^16.3.2",
    "langchain": "^0.0.208"
  }
}
